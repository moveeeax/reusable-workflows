# Drop this into any repo to cut a GitHub Release when you push a v* tag.
name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  release:
    uses: moveeeax/reusable-workflows/.github/workflows/release.yml@v1
    with:
      generate-notes: true
      files: |
        dist/*
